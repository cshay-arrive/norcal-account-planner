node_modules
dist
dist-ssr
.vercel
*.local
.DS_Store
.env
.env.*
.env*

# Real account books. These hold actual client revenue and must never be
# committed or bundled — the app loads them from disk at runtime instead.
private/
*.book.json
